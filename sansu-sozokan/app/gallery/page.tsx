"use client";
import { useState } from "react"; import { Heart, Repeat2 } from "lucide-react"; import { PageTitle } from "@/components/ui/page-title"; import { works } from "@/lib/mock-data";
export default function Gallery(){const [likes,setLikes]=useState<Record<string,number>>({});return <><PageTitle eyebrow="Gallery" title="私の創作展" description="作品の閲覧、いいね、リミックスを行う安全設計の学習SNSモック。投稿は教師・保護者の承認フローを想定します。"/><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{works.map(w=><article key={w.id} className="panel overflow-hidden"><div className="grid aspect-[4/3] place-items-center" style={{background:`linear-gradient(135deg,${w.colors.join(',')})`}}><div className="h-32 w-32 rotate-12 rounded-[36%] border-[18px] border-white/70"/></div><div className="p-5"><h2 className="text-xl font-black">{w.title}</h2><p className="mt-1 text-sm opacity-55">by {w.author}</p><div className="mt-5 flex gap-3"><button onClick={()=>setLikes(s=>({...s,[w.id]:(s[w.id]??0)+1}))} className="flex items-center gap-2 rounded-xl border border-[var(--line)] px-3 py-2 text-sm font-bold"><Heart className="h-4 w-4"/> {w.likes+(likes[w.id]??0)}</button><button className="flex items-center gap-2 rounded-xl bg-violet-600 px-3 py-2 text-sm font-bold text-white"><Repeat2 className="h-4 w-4"/>リミックス</button></div></div></article>)}</div></>}
