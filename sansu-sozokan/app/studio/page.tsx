"use client";
import { useRef,useState } from "react"; import { Eraser, Save, Sparkles } from "lucide-react"; import { PageTitle } from "@/components/ui/page-title"; import { useLearningStore } from "@/store/use-learning-store";
export default function Studio(){const canvas=useRef<HTMLCanvasElement>(null);const [status,setStatus]=useState("キャンバスをクリックして図形を配置");const addPoints=useLearningStore(s=>s.addPoints);function draw(e:React.MouseEvent<HTMLCanvasElement>){const c=canvas.current;if(!c)return;const r=c.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top,ctx=c.getContext('2d');if(!ctx)return;ctx.fillStyle=`hsl(${Math.random()*360} 75% 60%)`;ctx.beginPath();ctx.arc(x,y,15+Math.random()*35,0,Math.PI*2);ctx.fill();setStatus("図形を追加しました");}return <><PageTitle eyebrow="Creative Studio" title="自由創造営" description="Canvas APIを使った図形アートの動作モック。次段階でThree.jsやScratch風ブロックエディタを接続します。"/><div className="grid gap-6 lg:grid-cols-[1fr_320px]"><div className="panel p-4"><canvas ref={canvas} onClick={draw} width={900} height={560} className="h-[55vh] w-full cursor-crosshair rounded-2xl bg-white shadow-inner"/><p className="mt-3 text-sm opacity-60">{status}</p></div><aside className="space-y-4"><div className="panel p-5"><h2 className="font-black">ツールボックス</h2><div className="mt-4 grid grid-cols-2 gap-3"><button onClick={()=>canvas.current?.getContext('2d')?.clearRect(0,0,900,560)} className="rounded-xl border border-[var(--line)] p-3 text-sm font-bold"><Eraser className="mx-auto mb-2"/>消去</button><button onClick={()=>{addPoints(20);setStatus("作品を保存しました：+20pt")}} className="rounded-xl bg-violet-600 p-3 text-sm font-bold text-white"><Save className="mx-auto mb-2"/>保存</button></div></div><div className="panel p-5"><Sparkles className="text-amber-500"/><h3 className="mt-3 font-black">創作チャレンジ</h3><p className="mt-2 text-sm leading-6 opacity-65">円だけを使って、左右対称な生き物をつくろう。</p></div></aside></div></>}
