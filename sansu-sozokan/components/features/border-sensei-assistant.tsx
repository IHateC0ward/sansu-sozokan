"use client";
import { useState } from "react"; import { motion, AnimatePresence } from "motion/react"; import { X, Sparkles } from "lucide-react";
const tips=["まちがいは発見の入口だワン！","図にすると、見えなかった関係が見えるよ。","答えより先に『なぜ？』を考えてみよう！"];
export function BorderSenseiAssistant(){const [open,setOpen]=useState(false);const [tip,setTip]=useState(tips[0]);return <div className="fixed bottom-24 right-4 z-50"><AnimatePresence>{open&&<motion.div initial={{opacity:0,y:12,scale:.95}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:12,scale:.95}} className="mb-3 w-72 rounded-2xl border border-violet-300 bg-[var(--panel)] p-4 shadow-2xl"><button className="float-right" onClick={()=>setOpen(false)}><X className="h-4 w-4"/></button><p className="pr-6 text-sm font-bold">ボーダー先生</p><p className="mt-2 text-sm leading-6">{tip}</p><button onClick={()=>setTip(tips[Math.floor(Math.random()*tips.length)])} className="mt-3 flex items-center gap-1 text-xs font-bold text-violet-600"><Sparkles className="h-4 w-4"/>別のヒント</button></motion.div>}</AnimatePresence><motion.button whileHover={{scale:1.05}} whileTap={{scale:.95}} onClick={()=>setOpen(v=>!v)} className="grid h-16 w-16 place-items-center rounded-2xl border-4 border-white bg-slate-900 text-2xl shadow-xl" aria-label="ボーダー先生を開く">🐶</motion.button></div>}
