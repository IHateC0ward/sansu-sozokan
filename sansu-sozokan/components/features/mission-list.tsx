"use client";
import { CheckCircle2, Circle, Gift } from "lucide-react"; import { missions } from "@/lib/mock-data"; import { useLearningStore } from "@/store/use-learning-store";
export function MissionList(){const {completedMissionIds,completeMission}=useLearningStore();return <div className="space-y-3">{missions.map(m=>{const done=completedMissionIds.includes(m.id);return <button disabled={done} onClick={()=>completeMission(m.id,m.reward)} key={m.id} className="flex w-full items-center gap-3 rounded-2xl border border-[var(--line)] p-4 text-left transition hover:-translate-y-0.5 disabled:opacity-60">{done?<CheckCircle2 className="text-emerald-500"/>:<Circle className="text-violet-500"/>}<span className="flex-1 text-sm font-bold">{m.title}</span><span className="flex items-center gap-1 text-xs font-black text-amber-500"><Gift className="h-4 w-4"/>+{m.reward}</span></button>})}</div>}
