export type Mission = { id:string; title:string; reward:number; done:boolean };
export type Badge = { id:string; label:string; icon:string };
export type GalleryWork = { id:string; title:string; author:string; likes:number; colors:string[] };
